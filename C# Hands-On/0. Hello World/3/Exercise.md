# Your Task

Modify the code to:

- Keep the descriptive comments (they're just notes)
- **Comment out** the last `Console.WriteLine` so it doesn't execute

The output should only show "Hello, World!" and the number 8.

## Expected Output

```cs
Hello, World!
8
```

## Hints

💡 1. To comment out a line of code, add `//` at the very beginning of that line

💡 2. The line `Console.WriteLine("This should NOT print");` needs to be commented out.

💡 3. Comments starting with `//` make the entire rest of that line ignored by the computer.
