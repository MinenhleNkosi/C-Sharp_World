# Your Task

Create an integer array with exactly 5 elements using the `new` keyword, then print its length to the console.

## Method Signature

```cs
public static void PrintArrayLength()
```

## Expected Output

```
5
```

## Hints

- Use `new int[5]` to create an array that holds 5 integers
- Access the array's size using the `.Length` property
- Use `Console.WriteLine()` to print the length value

## Example Code

```cs
using System;

public class Solution
{
    public static void PrintArrayLength()
    {
        // Create an int array of size 5
        // Print its length using Console.WriteLine()

    }
}
```
