```cs
using System;

public class Solution
{
    public static int GetAge()
    {
        // Declare an int variable called 'age' and assign it the value 25
        int age = 25;
        // Return the age variable
        return age;
    }

    public static int GetYear()
    {
        // Declare an int variable called 'year' and assign it the value 2024
        int year = 2024;
        // Return the year variable
        return year;
    }

    public static int GetTemperature()
    {
        // Declare an int variable called 'temperature' and assign it the value -5
        int temperature = -5;
        // Return the temperature variable
        return temperature;
    }

    public static int AddNumbers(int a, int b)
    {
        // Return the sum of a and b
        return a + b;
    }
}
```
