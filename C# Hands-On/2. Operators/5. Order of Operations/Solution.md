```cs

using System;

public class Solution
{
    public static int Calculate(int a, int b, int c)
    {
        // Use parentheses to ensure (a + b) is calculated before multiplying by c
        return (a + b) * c;
    }
}

```
