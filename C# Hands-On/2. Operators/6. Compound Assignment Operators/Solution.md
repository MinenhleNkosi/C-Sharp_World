```cs

using System;

public class Solution
{
    // Use += to add amount to value and return the result
    public static int ApplyAdditionAssignment(int value, int amount)
    {
        // Your code here
        return value += amount;
    }

    // Use -= to subtract amount from value and return the result
    public static int ApplySubtractionAssignment(int value, int amount)
    {
        // Your code here
        return value -= amount;
    }

    // Use *= to multiply value by amount and return the result
    public static int ApplyMultiplicationAssignment(int value, int amount)
    {
        // Your code here
        return value *= amount;
    }

    // Use /= to divide value by amount and return the result
    public static int ApplyDivisionAssignment(int value, int amount)
    {
        // Your code here
        return value /= amount;
    }
}

```
